# This file is deprecated. Use the canonical docker-compose.yml at the repository root.
# 
# To use docker-compose from repo root:
#   docker-compose up -d
# 
# This file is kept for reference only. The canonical compose file is:
#   ../docker-compose.yml

# To use this file, run from infra/ directory:
#   docker-compose -f ../docker-compose.yml up -d



